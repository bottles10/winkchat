<!DOCTYPE html>
<html>
  <head>
    <title>Winkchat</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen">
    <header class="fixed top-0 w-full">
      <nav class=" bg-white p-5 mb-4 flex justify-between items-center shadow-md text-blue-400">
        <div class="">
          <h1 class="text-2xl hover:text-blue-300"><%= link_to "WinkChat", root_path %></h1>
        </div>
        <div class="flex items-center md:inline-block text-center space-x-6">
          <% if user_signed_in? %>
            <%= link_to "New Post", new_post_path, class: "border border-blue-400 hover:border-blue-600 py-2 w-24 md:w-32 rounded inline-block hover:text-blue-600 transition duration-300" %>
            <%= link_to "Logout", destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm: "Do you want to log out?"}, class: "border border-blue-400 hover:border-blue-600 py-2 w-24 md:w-32 rounded inline-block hover:text-blue-600 transition duration-300" %>
          <% else %>  
            <%= link_to "Login", new_user_session_path, class: "hidden border border-blue-400 hover:border-blue-600 py-2 w-24 md:32 rounded md:inline-block hover:text-blue-600 transition duration-300" %>
            <%= link_to "Register", new_user_registration_path, class: "hidden border border-blue-400 hover:border-blue-600 py-2 w-24 md:w-32 rounded md:inline-block hover:text-blue-600 transition duration-300" %>
        
            <!-- Mobile specific -->
            <%= link_to "⚙️", "#", class: "md:hidden text-2xl" %> <!-- For Account Settings -->
          <% end %>
        </div>
      </nav>
      
      <%= render "layouts/flashes" %>   
       
    </header>
    <main class="container mx-auto mt-28 px-5 text-gray-700">
      <%= yield %>
    </main>
  </body>
</html>
